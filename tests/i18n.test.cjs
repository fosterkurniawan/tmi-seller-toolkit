const test=require('node:test'),assert=require('node:assert/strict'),vm=require('node:vm'),fs=require('node:fs'),path=require('node:path');
const root=path.resolve(process.env.TOOLKIT_WEB||'web');
function locale(saved){const c={localStorage:{getItem:()=>saved}};vm.createContext(c);for(const file of ['i18n-zh.js','i18n.js'])vm.runInContext(fs.readFileSync(path.join(root,file),'utf8'),c);return c;}
test('Mandarin preference restores with Chinese locale and localized dynamic labels',()=>{const c=locale('zh');assert.equal(c.uiLocale(),'zh-CN');assert.equal(c.translate('Nama toko'),'店铺名称');assert.equal(c.translate('2/3 alat dijelajahi'),'已探索2/3个工具');assert.equal(c.translate('Info Premi seller'),'关于卖家保费');assert.equal(c.translate('Hapus ORDER-1'),'删除ORDER-1');assert.equal(c.translate('Harga Rp100,000 · HPP Rp55,000 · target 20%'),'售价Rp100,000 · 商品成本Rp55,000 · 目标20%');assert.equal(c.translate(1234),1234);assert.equal(c.translate('Rp29,700'),'Rp29,700');});
test('existing Indonesian and English preferences still work',()=>{assert.equal(locale('id').translate('Nama toko'),'Nama toko');assert.equal(locale('en').translate('Nama toko'),'Store name');assert.equal(locale('invalid').uiLocale(),'id-ID');});
test('Mandarin covers the English dictionary keys referenced by the current UI',()=>{const c=locale('zh');const source=['index.html','workspace.js','protection.js','pricing.js','access.html','access.js'].map(f=>fs.readFileSync(path.join(root,f),'utf8')).join('\n');c.source=source;const missing=vm.runInContext('Object.keys(EN).filter(k=>source.includes(k)&&!Object.hasOwn(ZH,k))',c);assert.deepEqual(Array.from(missing),[]);});
